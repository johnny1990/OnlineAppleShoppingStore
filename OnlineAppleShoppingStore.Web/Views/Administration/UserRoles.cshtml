@model IEnumerable<OnlineAppleShoppingStoreAuth.Entities.AspNetUserRole>

@{
    ViewBag.Title = "UserRoles";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("NewUserRole", "NewUserRole")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleId)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleId)
            </td>
            <td>
                @Html.ActionLink("Edit", "EditUserRole", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "DeleteUserRole", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
